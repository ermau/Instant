<Window x:Class="Instant.VisualStudio.TestCodeWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	mc:Ignorable="d" Width="400" Height="200" ResizeMode="NoResize"
	ShowInTaskbar="False" WindowStartupLocation="CenterOwner">
	
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="100" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		
		<TextBox Name="code" Grid.Row="0" AcceptsReturn="True" AcceptsTab="True" />
		
		<StackPanel Grid.Row="1" Orientation="Horizontal">
			<Label Target="{Binding ElementName=timeout}">Time_out (ms):</Label>
			<TextBox Name="timeout" Margin="4" Text="60000" Width="50" />
			<!--<Label Target="{Binding ElementName=loops}">_Max Loops:</Label>
			<TextBox Name="loops" Margin="4" Text="{Binding MaximumLoops}" Width="40" />-->
		</StackPanel>
		
		<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
			<Button Margin="5" MinWidth="50" IsDefault="True" Click="OnClickOk">Ok</Button>
			<Button Margin="5" MinWidth="50" IsCancel="True" Click="OnClickCancel">Cancel</Button>
		</StackPanel>
    </Grid>
</Window>
