<UserControl x:Class="Instant.VisualStudio.ExceptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
	<Border>
		<Expander Header="{Binding Exception.Message}">
			<ListBox ItemsSource="{Binding Frames}">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<TextBlock>
							<TextBlock.Text>
								<MultiBinding StringFormat="at {0} in {1}:line {2}">
									<Binding Path="Method" />
									<Binding Path="File" />
									<Binding Path="Line" />
								</MultiBinding>
							</TextBlock.Text>
						</TextBlock>
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
		</Expander>
	</Border>
</UserControl>
